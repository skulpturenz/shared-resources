diff --git a/deploy/docker-swarm/clickhouse-setup/clickhouse-storage.xml b/deploy/docker-swarm/clickhouse-setup/clickhouse-storage.xml
index 54ec4976..bacc8761 100644
--- a/deploy/docker-swarm/clickhouse-setup/clickhouse-storage.xml
+++ b/deploy/docker-swarm/clickhouse-setup/clickhouse-storage.xml
@@ -13,15 +13,15 @@
                 For GCS cold storage,
                     endpoint should be https://storage.googleapis.com/<bucket-name>/data/
                 -->
-            <endpoint>https://BUCKET-NAME.s3-REGION-NAME.amazonaws.com/data/</endpoint>
-            <access_key_id>ACCESS-KEY-ID</access_key_id>
-            <secret_access_key>SECRET-ACCESS-KEY</secret_access_key>
+            <endpoint>https://storage.googleapis.com/skulpture-shared-resources/telemetry/</endpoint>
+            <access_key_id>TODO</access_key_id>
+            <secret_access_key>TODO</secret_access_key>
             <!-- In case of S3, uncomment the below configuration in case you want to read
                 AWS credentials from the Environment variables if they exist. -->
             <!-- <use_environment_credentials>true</use_environment_credentials> -->
             <!-- In case of GCS, uncomment the below configuration, since GCS does
                 not support batch deletion and result in error messages in logs. -->
-            <!-- <support_batch_delete>false</support_batch_delete> -->
+            <support_batch_delete>false</support_batch_delete>
         </s3>
    </disks>
    <policies>
diff --git a/deploy/docker-swarm/clickhouse-setup/docker-compose.yaml b/deploy/docker-swarm/clickhouse-setup/docker-compose.yaml
index e6354bb3..93c47d57 100644
--- a/deploy/docker-swarm/clickhouse-setup/docker-compose.yaml
+++ b/deploy/docker-swarm/clickhouse-setup/docker-compose.yaml
@@ -16,14 +16,7 @@ x-clickhouse-defaults: &clickhouse-defaults
       max-file: "3"
   healthcheck:
     # "clickhouse", "client", "-u ${CLICKHOUSE_USER}", "--password ${CLICKHOUSE_PASSWORD}", "-q 'SELECT 1'"
-    test:
-      [
-        "CMD",
-        "wget",
-        "--spider",
-        "-q",
-        "0.0.0.0:8123/ping"
-      ]
+    test: [ "CMD", "wget", "--spider", "-q", "0.0.0.0:8123/ping" ]
     interval: 30s
     timeout: 5s
     retries: 3
@@ -147,11 +140,7 @@ services:
 
   query-service:
     image: signoz/query-service:0.46.0
-    command:
-      [
-        "-config=/root/config/prometheus.yml",
-        # "--prefer-delta=true"
-      ]
+    command: [ "-config=/root/config/prometheus.yml" ] # "--prefer-delta=true"
     # ports:
     #   - "6060:6060"     # pprof port
     #   - "8080:8080"     # query-service port
@@ -195,6 +184,8 @@ services:
       - query-service
     ports:
       - "3301:3301"
+      - "80:3301"
+      - "443:3301"
     volumes:
       - ../common/nginx-config.conf:/etc/nginx/conf.d/default.conf
 
@@ -219,10 +210,11 @@ services:
       # - "1777:1777"     # pprof extension
       - "4317:4317" # OTLP gRPC receiver
       - "4318:4318" # OTLP HTTP receiver
+      - "8880:4318" # Cloudflare supported OTLP HTTP receiver
+      - "13133:13133" # Health check extension
       # - "8888:8888"     # OtelCollector internal metrics
       # - "8889:8889"     # signoz spanmetrics exposed by the agent
       # - "9411:9411"     # Zipkin port
-      # - "13133:13133"   # Health check extension
       # - "14250:14250"   # Jaeger gRPC
       # - "14268:14268"   # Jaeger thrift HTTP
       # - "55678:55678"   # OpenCensus receiver
@@ -237,17 +229,17 @@ services:
       - query-service
 
   otel-collector-migrator:
-      image: signoz/signoz-schema-migrator:0.88.24
-      deploy:
-        restart_policy:
-          condition: on-failure
-          delay: 5s
-      command:
-        - "--dsn=tcp://clickhouse:9000"
-      depends_on:
-        - clickhouse
-        # - clickhouse-2
-        # - clickhouse-3
+    image: signoz/signoz-schema-migrator:0.88.24
+    deploy:
+      restart_policy:
+        condition: on-failure
+        delay: 5s
+    command:
+      - "--dsn=tcp://clickhouse:9000"
+    depends_on:
+      - clickhouse
+      # - clickhouse-2
+      # - clickhouse-3
 
   logspout:
     image: "gliderlabs/logspout:v3.2.14"
@@ -261,27 +253,3 @@ services:
       mode: global
       restart_policy:
         condition: on-failure
-
-  hotrod:
-    image: jaegertracing/example-hotrod:1.30
-    command: [ "all" ]
-    environment:
-      - JAEGER_ENDPOINT=http://otel-collector:14268/api/traces
-    logging:
-      options:
-        max-size: 50m
-        max-file: "3"
-
-  load-hotrod:
-    image: "signoz/locust:1.2.3"
-    hostname: load-hotrod
-    environment:
-      ATTACKED_HOST: http://hotrod:8080
-      LOCUST_MODE: standalone
-      NO_PROXY: standalone
-      TASK_DELAY_FROM: 5
-      TASK_DELAY_TO: 30
-      QUIET_MODE: "${QUIET_MODE:-false}"
-      LOCUST_OPTS: "--headless -u 10 -r 1"
-    volumes:
-      - ../common/locust-scripts:/locust
diff --git a/deploy/docker-swarm/clickhouse-setup/otel-collector-config.yaml b/deploy/docker-swarm/clickhouse-setup/otel-collector-config.yaml
index 32cd007d..058e845a 100644
--- a/deploy/docker-swarm/clickhouse-setup/otel-collector-config.yaml
+++ b/deploy/docker-swarm/clickhouse-setup/otel-collector-config.yaml
@@ -1,4 +1,11 @@
 receivers:
+  httpcheck:
+    targets:
+      - endpoint: "https://authn.skulpture.xyz/ping"
+        method: GET
+      - endpoint: "https://authz.skulpture.xyz/ping"
+        method: GET
+    collection_interval: 10s
   tcplog/docker:
     listen_address: "0.0.0.0:2255"
     operators:
@@ -24,6 +31,9 @@ receivers:
         endpoint: 0.0.0.0:4317
       http:
         endpoint: 0.0.0.0:4318
+        cors:
+          allowed_origins:
+            - "*"
   jaeger:
     protocols:
       grpc:
@@ -144,6 +154,7 @@ service:
       processors: [signozspanmetrics/cumulative, signozspanmetrics/delta, batch]
       exporters: [clickhousetraces]
     metrics:
+      receivers: [otlp, httpcheck]
       receivers: [otlp]
       processors: [batch]
       exporters: [clickhousemetricswrite]
diff --git a/deploy/docker/clickhouse-setup/clickhouse-storage.xml b/deploy/docker/clickhouse-setup/clickhouse-storage.xml
index 54ec4976..bacc8761 100644
--- a/deploy/docker/clickhouse-setup/clickhouse-storage.xml
+++ b/deploy/docker/clickhouse-setup/clickhouse-storage.xml
@@ -13,15 +13,15 @@
                 For GCS cold storage,
                     endpoint should be https://storage.googleapis.com/<bucket-name>/data/
                 -->
-            <endpoint>https://BUCKET-NAME.s3-REGION-NAME.amazonaws.com/data/</endpoint>
-            <access_key_id>ACCESS-KEY-ID</access_key_id>
-            <secret_access_key>SECRET-ACCESS-KEY</secret_access_key>
+            <endpoint>https://storage.googleapis.com/skulpture-shared-resources/telemetry/</endpoint>
+            <access_key_id>TODO</access_key_id>
+            <secret_access_key>TODO</secret_access_key>
             <!-- In case of S3, uncomment the below configuration in case you want to read
                 AWS credentials from the Environment variables if they exist. -->
             <!-- <use_environment_credentials>true</use_environment_credentials> -->
             <!-- In case of GCS, uncomment the below configuration, since GCS does
                 not support batch deletion and result in error messages in logs. -->
-            <!-- <support_batch_delete>false</support_batch_delete> -->
+            <support_batch_delete>false</support_batch_delete>
         </s3>
    </disks>
    <policies>
diff --git a/deploy/docker/clickhouse-setup/docker-compose.yaml b/deploy/docker/clickhouse-setup/docker-compose.yaml
index 12b91b69..91d79892 100644
--- a/deploy/docker/clickhouse-setup/docker-compose.yaml
+++ b/deploy/docker/clickhouse-setup/docker-compose.yaml
@@ -15,14 +15,7 @@ x-clickhouse-defaults: &clickhouse-defaults
       max-file: "3"
   healthcheck:
     # "clickhouse", "client", "-u ${CLICKHOUSE_USER}", "--password ${CLICKHOUSE_PASSWORD}", "-q 'SELECT 1'"
-    test:
-      [
-        "CMD",
-        "wget",
-        "--spider",
-        "-q",
-        "0.0.0.0:8123/ping"
-      ]
+    test: [ "CMD", "wget", "--spider", "-q", "0.0.0.0:8123/ping" ]
     interval: 30s
     timeout: 5s
     retries: 3
@@ -166,11 +159,7 @@ services:
   query-service:
     image: signoz/query-service:${DOCKER_TAG:-0.46.0}
     container_name: signoz-query-service
-    command:
-      [
-        "-config=/root/config/prometheus.yml",
-        # "--prefer-delta=true"
-      ]
+    command: [ "-config=/root/config/prometheus.yml" ] # "--prefer-delta=true"
     # ports:
     #   - "6060:6060"     # pprof port
     #   - "8080:8080"     # query-service port
@@ -211,6 +200,8 @@ services:
       - query-service
     ports:
       - "3301:3301"
+      - "80:3301"
+      - "443:3301"
     volumes:
       - ../common/nginx-config.conf:/etc/nginx/conf.d/default.conf
 
@@ -251,10 +242,11 @@ services:
       # - "1777:1777"     # pprof extension
       - "4317:4317" # OTLP gRPC receiver
       - "4318:4318" # OTLP HTTP receiver
+      - "8880:4318" # Cloudflare supported OTLP HTTP receiver
+      - "13133:13133" # health check extension
       # - "8888:8888"     # OtelCollector internal metrics
       # - "8889:8889"     # signoz spanmetrics exposed by the agent
       # - "9411:9411"     # Zipkin port
-      # - "13133:13133"   # health check extension
       # - "14250:14250"   # Jaeger gRPC
       # - "14268:14268"   # Jaeger thrift HTTP
       # - "55678:55678"   # OpenCensus receiver
@@ -278,29 +270,3 @@ services:
     depends_on:
       - otel-collector
     restart: on-failure
-
-  hotrod:
-    image: jaegertracing/example-hotrod:1.30
-    container_name: hotrod
-    logging:
-      options:
-        max-size: 50m
-        max-file: "3"
-    command: [ "all" ]
-    environment:
-      - JAEGER_ENDPOINT=http://otel-collector:14268/api/traces
-
-  load-hotrod:
-    image: "signoz/locust:1.2.3"
-    container_name: load-hotrod
-    hostname: load-hotrod
-    environment:
-      ATTACKED_HOST: http://hotrod:8080
-      LOCUST_MODE: standalone
-      NO_PROXY: standalone
-      TASK_DELAY_FROM: 5
-      TASK_DELAY_TO: 30
-      QUIET_MODE: "${QUIET_MODE:-false}"
-      LOCUST_OPTS: "--headless -u 10 -r 1"
-    volumes:
-      - ../common/locust-scripts:/locust
diff --git a/deploy/docker/clickhouse-setup/otel-collector-config.yaml b/deploy/docker/clickhouse-setup/otel-collector-config.yaml
index 7a103d10..724452fb 100644
--- a/deploy/docker/clickhouse-setup/otel-collector-config.yaml
+++ b/deploy/docker/clickhouse-setup/otel-collector-config.yaml
@@ -1,4 +1,11 @@
 receivers:
+  httpcheck:
+    targets:
+      - endpoint: "https://authn.skulpture.xyz/ping"
+        method: GET
+      - endpoint: "https://authz.skulpture.xyz/ping"
+        method: GET
+    collection_interval: 10s
   tcplog/docker:
     listen_address: "0.0.0.0:2255"
     operators:
@@ -24,6 +31,9 @@ receivers:
         endpoint: 0.0.0.0:4317
       http:
         endpoint: 0.0.0.0:4318
+        cors:
+          allowed_origins:
+            - "*"
   jaeger:
     protocols:
       grpc:
@@ -151,7 +161,7 @@ service:
       processors: [signozspanmetrics/cumulative, signozspanmetrics/delta, batch]
       exporters: [clickhousetraces]
     metrics:
-      receivers: [otlp]
+      receivers: [otlp, httpcheck]
       processors: [batch]
       exporters: [clickhousemetricswrite]
     metrics/generic:
