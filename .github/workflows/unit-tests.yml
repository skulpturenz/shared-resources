name: Unit tests

on:
  pull_request:
    branches: [master, staging, dev]
  workflow_dispatch:

jobs:
  kryptos:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: stable
    
      - name: Unit test (commands)
        run: go test -cover
        working-directory: secrets/commands